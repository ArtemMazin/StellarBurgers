export const ingredients = (state) => state.initialIngredients.initialIngredients;
export const status = (state) => state.initialIngredients.status;
export const error = (state) => state.initialIngredients.error;
